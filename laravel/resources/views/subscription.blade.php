@extends('layouts.app') @section('content')<p class="text-indigo-600 font-bold text-xs uppercase tracking-widest">Subscription</p><h1 class="text-4xl font-black">Choose what fits your school</h1><p class="text-slate-500 mt-2">{{ $usage }} / {{ $school->studentLimit() ?? 'Unlimited' }} students used</p><div class="grid md:grid-cols-3 gap-5 mt-8">@foreach($plans as $key=>$plan)<article class="bg-white border {{ $school->tier===$key?'ring-2 ring-indigo-600':'' }} rounded-2xl p-6"><p class="font-bold text-indigo-600">{{ $plan['label'] }}</p><h2 class="text-3xl font-black mt-2">₦{{ number_format($plan['price']) }}<small class="text-sm text-slate-400">/year</small></h2><p class="my-5 text-slate-500">{{ $plan['student_limit'] ?? 'Unlimited' }} students</p><ul class="space-y-2">@foreach($plan['features'] as $feature)<li>✓ {{ ucwords(str_replace('_',' ',$feature)) }}</li>@endforeach</ul><button class="mt-6 w-full bg-indigo-600 text-white rounded-xl p-3">{{ $school->tier===$key?'Current plan':'Upgrade' }}</button></article>@endforeach</div>@endsection
